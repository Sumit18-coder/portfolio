Add these real files here before deploying:

- resume.pdf — your actual resume, linked from the nav, hero, and command palette.
- og-image.png — 1200x630 image used for social share previews (LinkedIn, Twitter/X).
- favicon.ico — browser tab icon.

None of these block `npm run build`, but the site will 404 on them until they're added.
